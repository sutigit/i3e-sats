export default function SatDetail() {
    return (
        <div id="right-panel">
            <div className="content place-center" >

                {/* <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
                    <p style={{ fontSize: '1.1rem', fontWeight: 'bold' }}>{data.name}</p>
                    <div style={{ background: '#333', padding: '2px 8px', borderRadius: '4px', fontSize: '0.8rem' }}>
                        Look: <span style={{ color: '#2dd4bf' }}>{s.look.compass}</span>
                    </div>
                </div>

                <div style={{ display: 'flex', gap: '1rem', marginTop: '1rem', flexWrap: 'wrap' }}>

                    <Measure label="Lat" value={s.location.lat} unit="°" />
                    <Measure label="Lon" value={s.location.lon} unit="°" />
                    <Measure label="Alt" value={s.location.altitude} unit="km" />

                    <Measure label="Elev" value={s.look.elevation} unit="°" />
                    <Measure label="Azim" value={s.look.azimuth} unit="°" />

                    <Measure label="Dist" value={s.look.range} unit="km" />
                    <Measure label="Speed" value={s.physics.velocity} unit="km/s" />

                    <Measure
                        label="Doppler"
                        value={s.physics.rangeRate}
                        unit="km/s"
                        color={dopplerColor}
                    />
                </div> */}
            </div>
        </div>
    )
}
